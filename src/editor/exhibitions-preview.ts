import { randomUUID } from "node:crypto";
import type { ExhibitionLocale, ExhibitionLocalized, ExhibitionShared } from "../content-loaders/exhibitions/schema.ts";
import { validateExhibitionsEditorDraft, type ExhibitionsEditorDraftState } from "./exhibitions-draft-state.ts";
export type ExhibitionsPreviewModel={contentId:string;locale:ExhibitionLocale;shared:ExhibitionShared;localized:ExhibitionLocalized;body:string};
export class ExhibitionsPreviewError extends Error { readonly code:"invalid-request"|"preview-blocked"|"preview-not-found"|"preview-expired";constructor(message:string,code:ExhibitionsPreviewError["code"]){super(message);this.code=code;} }
export function createExhibitionsPreviewModel(draft:ExhibitionsEditorDraftState,locale:ExhibitionLocale="ja"):ExhibitionsPreviewModel{if(locale!=="ja"&&locale!=="en")throw new ExhibitionsPreviewError("Invalid locale","invalid-request");if(!validateExhibitionsEditorDraft(draft).capabilities.preview[locale])throw new ExhibitionsPreviewError("Preview blocked","preview-blocked");if(draft.shared.state!=="editable"||draft.locales[locale].state!=="editable")throw new ExhibitionsPreviewError("Source unavailable","preview-blocked");const {body,...localized}=draft.locales[locale].value;return{contentId:draft.contentId,locale,shared:structuredClone(draft.shared.value),localized:structuredClone(localized),body};}
export class ExhibitionsPreviewStore{private records=new Map<string,{model:ExhibitionsPreviewModel;expires:number}>();private ttl:number;private now:()=>number;constructor(ttl=600000,now:()=>number=Date.now){this.ttl=ttl;this.now=now;}create(model:ExhibitionsPreviewModel){const token=randomUUID();this.records.set(token,{model:structuredClone(model),expires:this.now()+this.ttl});return token;}read(token:string,localeOrId:string){const record=this.records.get(token);if(!record)throw new ExhibitionsPreviewError("Preview not found","preview-not-found");if(record.expires<=this.now()){this.records.delete(token);throw new ExhibitionsPreviewError("Preview expired","preview-expired");}if(localeOrId!==record.model.locale&&localeOrId!==record.model.contentId)throw new ExhibitionsPreviewError("Preview not found","preview-not-found");return structuredClone(record.model);}}
export const exhibitionsPreviewStore=new ExhibitionsPreviewStore();
